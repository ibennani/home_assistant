# Arkiv

Filer här används inte av live Home Assistant — de behålls som referens eller historik.

Tidigare `ui-lovelace.yaml` (ersatt dashboard) har tagits bort. Live-dashboarden **Översikt** versioneras i `dashboards/dashboard-september-2025.yaml` och laddas via `includes/lovelace.yaml`.
