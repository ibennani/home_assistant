# Sidomeny: Översikt först, Kartor dold för alla utom Ilias.
# Kräver custom-sidebar (HACS) + extra_module_url i configuration.yaml.
order:
  - item: /dashboard-september-2025
    match: href
  - item: /map
    match: href
    hide: true

exceptions:
  - user: Ilias Bennani
    extend_from: base
    order:
      - item: /dashboard-september-2025
        match: href
      - item: /map
        match: href
        hide: false
